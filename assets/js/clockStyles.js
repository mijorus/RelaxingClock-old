import * as classicClock from "./clockStyles/classicClock";
import * as analogClock  from "./clockStyles/analogClock";
import * as globeClock   from "./clockStyles/globeClock";
import * as metroClock   from "./clockStyles/metroClock";
import * as focusedClock from "./clockStyles/focusedClock";

export const clockStyles = {
    classicClock,
    analogClock,
    globeClock,
    metroClock,
    focusedClock,
}