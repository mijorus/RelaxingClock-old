{
  "name": "relaxingclockhugo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "netlify build && netlify dev -p 3000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "netlify-cli": "^3.2.0"
  }
}
